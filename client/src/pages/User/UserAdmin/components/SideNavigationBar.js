import { Card } from "@mui/material";

const SideNavigationBar = () => {
    return (
        <Card 
            sx={{
                height: '100%',
                width: '100%',
                backgroundColor: 'rgba(50,50,50,0.9)'
            }}
        >
            THis is SideNavigationBar
        </Card>
    );
}

export default SideNavigationBar;